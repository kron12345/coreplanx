# Codex Prompt: Assistant Documentation Assets

## Purpose
Keep assistant help images available via frontend assets.

## Context
- Related spec: `/specs/assistant-doc-assets.md` (Rules: R1, R2, R3)

## Instructions
- Ensure doc images resolve under `/assets/...` (R1).
- Store referenced images under `frontend/public/assets/` using the same paths (R2).
- Validate no 404s in help UI (R3).

## Expected Output
- Docs and assets aligned so images load in the UI.
